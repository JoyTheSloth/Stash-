---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/src/main.rs
  - src-tauri/src/db.rs
  - src/App.tsx
  - src/index.css
  - src/main.tsx
  - index.html
autonomous: true
requirements: [STOR-01, UI-02]

must_haves:
  truths:
    - "Application compiles and boots as a desktop app."
    - "Tauri SQLite database initializes successfully on startup."
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri configuration and build settings"
    - path: "src-tauri/src/main.rs"
      provides: "Tauri rust backend entrypoint"
    - path: "src-tauri/src/db.rs"
      provides: "SQLite database setup and migration schema"
    - path: "src/App.tsx"
      provides: "Frontend UI landing page structure"
---

<objective>
Scaffold the Tauri, React, Tailwind CSS, and SQLite workspace structure to establish a working desktop application baseline.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Tauri + React Workspace scaffolding</name>
  <files>package.json, index.html, src/main.tsx, src/App.tsx, src/index.css, src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/src/main.rs</files>
  <read_first>package.json</read_first>
  <action>Create the boilerplate files for a Tauri v1 or v2 application with a React SPA frontend and Rust backend. Set up package.json dependencies for React, Tailwind, and Tauri APIs. Configure Tauri configuration file with the package name 'clipbrain' and SQLite DB dependencies in Cargo.toml.</action>
  <verify>Check that the configuration and Cargo files exist and contain appropriate dependencies</verify>
  <acceptance_criteria>
    - package.json contains "react", "react-dom", and "@tauri-apps/api"
    - src-tauri/tauri.conf.json has package identifier "com.clipbrain.app"
    - src-tauri/Cargo.toml has dependencies for "tauri" and "rusqlite"
  </acceptance_criteria>
  <done>Boilerplate scaffolding is created</done>
</task>

<task type="auto">
  <name>Task 2: Setup SQLite local database connection & schema</name>
  <files>src-tauri/src/db.rs, src-tauri/src/main.rs</files>
  <read_first>src-tauri/Cargo.toml</read_first>
  <action>Create src-tauri/src/db.rs to handle SQLite database initialization, creating the database file in the user's application data directory. Define initial schema migrations to create the 'clipboard_items' table with fields: id (TEXT/UUID), content (TEXT), category (TEXT), source_app (TEXT), is_favorite (INTEGER), created_at (DATETIME).</action>
  <verify>Check that db.rs initializes connection and contains CREATE TABLE statement</verify>
  <acceptance_criteria>
    - src-tauri/src/db.rs contains "CREATE TABLE IF NOT EXISTS clipboard_items"
    - src-tauri/src/db.rs exports an initialize_db function
  </acceptance_criteria>
  <done>Database connection and tables setup logic works</done>
</task>

</tasks>

<verification>
- [ ] Tauri configurations are valid.
- [ ] Database schema is defined and db connection initializes.
</verification>

<success_criteria>
- Project structure successfully scaffolded.
- SQLite database initializes properly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
