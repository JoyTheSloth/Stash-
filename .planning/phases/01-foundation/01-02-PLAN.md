---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src-tauri/src/main.rs
  - src-tauri/src/clipboard.rs
  - src-tauri/src/db.rs
autonomous: true
requirements: [CLIP-01, STOR-01]

must_haves:
  truths:
    - "Clipboard updates are successfully intercepted by background loop."
    - "Copied items are written to SQLite database in real time."
  artifacts:
    - path: "src-tauri/src/clipboard.rs"
      provides: "Background clipboard listener and polling logic"
---

<objective>
Implement background system tray integration and a clipboard monitoring thread that writes copied items directly to the SQLite database.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create background clipboard listener thread</name>
  <files>src-tauri/src/clipboard.rs, src-tauri/src/main.rs</files>
  <read_first>src-tauri/src/main.rs</read_first>
  <action>Create src-tauri/src/clipboard.rs. Implement a loop that polls the system clipboard (using the 'arboard' crate or similar clipboard access library) every 500-1000ms. If the text content has changed and is not empty, trigger a write to the database.</action>
  <verify>Check that clipboard.rs compiles and contains a background loop</verify>
  <acceptance_criteria>
    - src-tauri/src/clipboard.rs has a monitor_clipboard function
    - src-tauri/src/main.rs spawns the clipboard monitor thread during setup
  </acceptance_criteria>
  <done>Background clipboard listener is running</done>
</task>

<task type="auto">
  <name>Task 2: Setup System Tray context menu</name>
  <files>src-tauri/src/main.rs, src-tauri/tauri.conf.json</files>
  <read_first>src-tauri/src/main.rs</read_first>
  <action>Configure Tauri system tray. Create standard tray menu with 'Show ClipBrain' and 'Quit' options. Set up window hiding on start and window show when clicking 'Show ClipBrain' or when clicking the tray icon.</action>
  <verify>Verify tauri.conf.json or main.rs contains SystemTray setup code</verify>
  <acceptance_criteria>
    - src-tauri/src/main.rs contains system tray initialization code
    - tauri.conf.json has systemTray or trayIcon config enabled
  </acceptance_criteria>
  <done>System tray app integration complete</done>
</task>

</tasks>

<verification>
- [ ] Rust compiler passes compilation of system tray and clipboard logic.
- [ ] SQLite database writes happen when simulated copies occur.
</verification>

<success_criteria>
- Background clipboard tracking is functional.
- Tauri tray application runs correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
